import { h, FunctionComponent } from 'preact'
import { memo } from 'preact/compat'

const PaperIdFlowSelector: FunctionComponent = () => (
  <div>PaperIdFlowSelector</div>
)

export default memo(PaperIdFlowSelector)
