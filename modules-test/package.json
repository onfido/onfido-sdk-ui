{
  "scripts": {
    "build": "npm-run-all build:*",
    "build:analytics": "cd AnalyticsCore && npm run build",
    "build:network": "cd network && npm run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
